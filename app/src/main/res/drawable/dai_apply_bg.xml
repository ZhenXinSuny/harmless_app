<?xml version="1.0" encoding="utf-8"?>
<!-- shape定义形状，shape="rectangle"表示形状为长方形 -->
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >
    <!-- 设置框内填充颜色 -->
    <solid android:color="#ffffff" />

    <!-- 设置圆角半径 -->
    <corners android:radius="3dp" />
    <!-- 设置边距 -->
    <padding
        android:bottom="2dp"
        android:left="10dp"
        android:right="10dp"
        android:top="2dp" />
    <!-- 设置渐变角度angle和渐变颜色 -->
    <gradient
        android:angle="360"
        android:endColor="#97EDE3"
        android:startColor="@color/J3" />
    <!-- 设置各边倒角大小 -->
    <corners
        android:bottomLeftRadius="120dp"
        android:bottomRightRadius="120dp"
        android:topLeftRadius="120dp"
        android:topRightRadius="120dp" />
</shape>